# IDE / Editor
.vscode/**
.idea/
.cursor/**
# _bmad/**

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Node (if any frontend tooling)
node_modules/
dist/

# Playwright（浏览器 E2E 运行产物）
test-results/
playwright-report/

# pytest-benchmark 每次运行的临时产物（baseline.json 入库，last.json 不入库）
benchmarks/last.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
.uv/
*.egg-info/
*.egg

# Environment & secrets
.env
.env.*
!.env.example
!.env.docker.example
!.env.production.example

# Config (keep samples, ignore real secrets)
src/config/*.yaml
src/config/*.yml
!src/config/*.sample.yaml
!src/config/*.sample.yml

# Data output (local only — keep directory structure via .gitkeep)
data/*
!data/.gitkeep
**/data/*
!**/data/.gitkeep
!**/data/sw_seed.sql


## AI 工具产物（.vscode/.cursor 见文件头部 IDE 段；data/ 见上方 data/* 规则）
.claude/**
.pi/
.zcode/
.playwright-mcp/
.gemini/
.video_agent/
.superpowers/
## Build artifacts (test coverage / TS incremental build — regenerated locally)
backend/.coverage
auth-service/.coverage
frontend/tsconfig.tsbuildinfo
!**/data/sw_custom_tags_seed.sql

# git worktrees
.worktrees/

# 本机专属 compose 覆盖（卷名等环境差异），不入库
docker-compose.override.yml
